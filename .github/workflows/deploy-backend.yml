name: Deploy Backend to Ubuntu Server

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      - '.github/workflows/deploy-backend.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: |
          cd backend
          npm install

      - name: Build backend
        run: |
          cd backend
          npm run build

      - name: Deploy to Ubuntu Server
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.SERVER_HOST || '34.226.234.127' }}
          username: ubuntu  
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT || '22' }}
          script: |
            cd //home/ubuntu/memora/backend
            echo "${{ secrets.BACKEND_ENV }}" > .env
            git pull origin main
            cd backend
            npm install
            npm run build
            pm2 restart memora-backend || pm2 start dist/index.js --name memora-backend || pm2 start dist/index.js --name memora-backend --watch
            pm2 save

      - name: Notify deployment success
        run: echo "Backend successfully deployed to Ubuntu server"
